package ${groupId}.boot;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * ${name}启动类
 *
 * @author gemini 自动生成
 * @version ${version}
 */
@SpringBootApplication
public class ${appNameUpper}BootApplication {
    /**
     * ${name}启动入口
     *
     * @param args 启动参数
     */
    public static void main(String[] args) {
        SpringApplication.run(${appNameUpper}BootApplication.class, args);
    }
}