package ${groupId}.boot.web.converter;

import com.gls.gemini.common.core.base.BaseConverter;
import ${groupId}.boot.web.entity.${entityNameUpper}Entity;
import ${groupId}.sdk.vo.${entityNameUpper}Vo;
import org.mapstruct.Mapper;
import org.mapstruct.MappingConstants;
import org.mapstruct.NullValuePropertyMappingStrategy;

/**
 * ${tableComment} 转换器
 *
 * @author gemini 自动生成
 * @version ${version}
 * @date ${date}
 */
@Mapper(componentModel = MappingConstants.ComponentModel.SPRING,
        nullValuePropertyMappingStrategy = NullValuePropertyMappingStrategy.IGNORE)
public interface ${entityNameUpper}Converter extends BaseConverter<${entityNameUpper}Vo, ${entityNameUpper}Entity> {
}
